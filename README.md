### 📋 自律移動ロボットにおける人流経路予測および人流回避アルゴリズム 統合評価表

| カテゴリ | モデル名 | 入力 | URL / リポジトリ | 🌟社会力（相互作用の考慮） | 🌟エッジ適性（リアルタイム性） | メリット・推奨理由 | デメリット・課題 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **古典的物理・数理** | **SFM** (Social Force Model) | 2D | PySocialForce | パーソナルスペース維持のための反発力を数式としてモデリングしているが、意図や譲り合いといった高次な表現力は弱い。 | 単純なベクトル演算のみのため、CPU環境でもミリ秒単位での実行が可能で極めて高い。 | 【Sim標準】群衆シミュレータの標準エンジンとして機能。計算が極めて軽い。 | 反応型で予見的回避不可。狭所で不自然な振動（オシレーション）や膠着（デッドロック）に陥りやすい。 |
| **古典的物理・数理** | **VO** (Velocity Obstacle) | 2D | (概念モデル) | 静的な回避になりがち（ORCAの前身）。 | 高速計算可能。 | 衝突する速度領域を幾何学的に定義。 | 単体での実装は稀。 |
| **古典的物理・数理** | **ORCA / RVO2** | 2D | RVO2 Library | 「互いに避け合う（責任折半）」という社会的な暗黙のルールを数学的な制約（速度半平面）に落とし込んでいる（中程度）。 | 線形計画法のソルバが極めて軽量。数千エージェントでも数ミリ秒で計算可能で、極めて高い。 | 【2D決定版】数学的に衝突フリーを保証し振動を抑制。AI暴走を防ぐ最下層の安全フィルタとして必須。 | 相手が協力的でない（スマホ歩き等）と機能不全に陥り、不自然な急制動を引き起こすリスクがある。 |
| **古典的物理・数理** | **DWA** (Dynamic Window Approach) | 2D | ROS Navigation | 歩行者を動的な障害物として扱うのみで、相互作用のモデリングは存在しない（無）。 | サンプリングベースの探索で計算負荷は低く、軽量エッジデバイスでも動作する。 | 【ROS標準】ロボットのキネマティクス（速度/加速度）制約を厳密に考慮し、実機追従性が高い。 | 動的障害物の未来予測能力がなく、歩行者が近づくとすぐに急停止する「Freezing Robot Problem」を引き起こしやすい。 |
| **古典的物理・数理** | **TEB** (Timed Elastic Band) | 2D | teb_local_planner | 歩行者を動的な障害物ペナルティとして扱うのみで、相互作用のモデリングは弱い。 | グラフ最適化の反復計算コストが高く、エッジデバイスでは最適化がタイムアウトし処理遅延がボトルネックになり得る。 | 【滑らかさ重視】キネマティクス制約を満たしながら、経路をゴムバンドのように変形させ時間最適な軌道を生成。 | 複雑な環境では局所解に陥りやすい（一度生成されたトポロジーから抜け出せない）。 |
| **古典的物理・数理** | **CBF** (Control Barrier Functions) | 2D/3D | CBF_QP_safety_filter | 数学的な安全制約として機能。 | 安全フィルタとして計算コストは極めて低い。 | 【安全性保証】数理的に安全領域を担保し、AIの予期せぬ暴走を防ぐフェールセーフとして機能。 | 設定次第で挙動が保守的になりすぎる可能性がある。 |
| **古典的物理・数理** | **DRA-MPPI** | 2D/3D | DRA-MPPI | 外部の深層予測モデルが出力する不確実な未来分布をコスト関数に直接組み込める（外部依存で高い）。 | 毎ステップ数百の軌道を評価しモンテカルロ積分を行うため、GPU並列演算（CUDA）が事実上必須（中程度）。 | 【制御SOTA】非ガウス型の動的障害物予測に対応し、過度に保守的な挙動やFreezing Robotを大幅に軽減。 | サンプリングと積分による計算負荷が極めて高く、エッジリソースを大きく消費する。 |
| **古典的物理・数理** | **Boids** | - | PredatorPrey (GitHub) | 距離に基づく分離・整列・結合の力のみで、高次な社会的ルール（パーソナルスペースや譲り合い）はない（低い）。 | 単純なベクトル演算でパラメータもごくわずか。空間ハッシュ等を用いれば数千エージェントでも極めて高速。 | 局所的ルールの積み重ねで、滑らかに障害物を避け合流する組織化された創発的振る舞いを極低計算で再現。 | 各個体に明確な目的地がなく局所ルールのみに依存するため、迷路やデッドエンドからの脱出が困難。 |
| **古典的物理・数理** | **セルオートマトン(フロアフィールド)** | 2D Map | - | 仮想フェロモン（動的フロアフィールド）により「流れに乗る」等の暗黙の群衆ルールを間接的にモデリング（中程度）。 | 局所的ルールの確率的遷移の並列計算であり、GPUを用いた並列化と極めて相性が良く非常に軽い。 | レーン形成やアーチ形成など、マクロな自己組織化現象の再現に非常に優れる。 | 空間・時間の離散化により軌道が不自然であり、ロボット制御に直接用いるには平滑化処理が必須。 |
| **伝統的機械学習** | **KF / EKF** (Kalman Filter) | - | FilterPy | - | 非常に軽量。 | 【状態推定】ノイズ除去と平滑化の基本技術。 | 急な意図変化（ターン等）には追従できない。 |
| **伝統的機械学習** | **GP** (Gaussian Processes) | 2D | GPy | 単一エージェントの時系列モデリングに留まり、相互作用の把握は困難。 | 計算量がデータ数の3乗で増加するため、オンラインでのエッジ実行は事実上困難。 | 【不確実性】予測の「確信度（分散）」を数学的に厳密に出力可能。 | 計算量の爆発によりリアルタイム処理が極めて困難。 |
| **伝統的機械学習** | **IRL** (Inverse RL) | 2D | Social-Nav-RL | 人間の軌跡からパーソナルスペース等の社会的規範をコスト関数として逆推定する強力な枠組み（強い）。 | オンラインで動的環境に適応しながら強化学習ループを解くことは計算コスト上非現実的。 | 【規範学習】人間らしい（Socially compliant）ナビゲーションの規範を抽出できる。 | 学習・推論プロセスにおける計算コストが極大。 |
| **伝統的機械学習** | **HMM** (Hidden Markov Model) | 2D | - | 単一意図推論に特化し、多人数間の相互作用空間の表現は次元の呪いにより困難（弱い）。 | オフライン学習は重いが、オンライン推論（Viterbiアルゴリズム）は軽量行列演算でCPUでも高い。 | センサノイズにロバストで、急な意図変化を確率遷移として自然に扱える。 | マルコフ性の仮定により長期予測で誤差が発散しやすく、離散化の粒度設定が難しい。 |
| **深層予測・生成** | **Social-LSTM** | 2D | Traj Pred Models | Social Poolingにより周囲の隠れ状態を集約。 | 逐次処理とグリッドプーリングの計算が重く、エッジ向きではない。 | RNNベースの軌跡予測の先駆的モデル。 | 計算が重く、多様性表現（Multimodality）に乏しい。 |
| **深層予測・生成** | **Social-GAN** | 2D | sgan | Social-LSTM同様のプーリング機構。 | 歩行者数増大で推論負荷が線形以上に増大し、エッジに不向き。 | ノイズベクトルとGANにより複数の未来軌跡を生成可能。 | 学習プロセスが不安定で、常に同じ軌跡しか出さないモード崩壊のリスクがある。 |
| **深層予測・生成** | **SoPhie** | 2D+Map | sophie (GitHub) | Social / Physical Attentionにより、人間同士だけでなく環境との複合的な暗黙のルールを高度に捉える（極めて強い）。 | 画像処理(CNN)とLSTM計算が重く、CPUエッジでは数十〜数百msの遅延。GPUが必須（低〜中）。 | 物理的制約（壁抜け等）を大幅削減し、障害物回避におけるマルチモーダルな歩行者挙動を再現。 | アーキテクチャが重厚で実行リソースを多く消費する。 |
| **深層予測・生成** | **Social-BiGAT** | 2D+Map | - | グラフアテンションにより「遠いが衝突軌道にある人」へ動的に強いアテンションを割り当てる（極めて強い）。 | LSTM+GAT+GANの重層構造が並列化を阻害し、制御周期への追従が極めて困難（低い）。 | 情報欠落を防ぎ、モード崩壊を回避して非常に多様で現実的な未来の軌跡を予測。 | モデルが極めて複雑で計算コストが膨大。 |
| **深層予測・生成** | **STAR** | 2D | STAR (GitHub) | 空間と時間独立のTransformerにより、対向・並走などの空間的インタラクションを効果的にモデリング（強い）。 | RNN排除で並列化効率は高いが、密集環境ではアテンション計算がO(N^2)で増大するため強力なGPUが必要（中〜高）。 | 長期予測精度に優れ、外部グラフメモリにより遮蔽等の一時的影響を滑らかに維持。 | パラメータ数が多く、密集環境での計算量増加が著しい。 |
| **深層予測・生成** | **PECNet** | 2D | PECNet (GitHub) | 他者の「予測された終点」を考慮したソーシャルプーリングを行うが、微細な相互作用解像度はやや劣る（中程度）。 | サンプリングと中間軌跡の一括生成は比較的軽量で安定している（中程度）。 | 目的地（終点）を先に予測することで長期予測の誤差発散を根本から防ぐ。デッドロック回避のプランナーにも適す。 | 終点予測が外れると中間軌跡が乖離し、歩行者の突発的な目的地変更への追従性が低い。 |
| **深層予測・生成** | **Social-STGCNN** | 2D | Social-STGCNN | 空間的グラフ構造として明示的に相互作用を表現するが、単純な距離ベースのため複雑な関係性は捉えきれない（高）。 | RNN排除で極めて軽く、パラメータ7.6K、500Hz推論。非力なエッジでも余裕で動作（極めて高い）。 | 【高速】計算速度が圧倒的で、予測エンジンとしてDWA等との組み合わせで現在最もバランスが良い。 | 単純な距離ベースのため、Transformer系等に比べ長期予測精度が劣る傾向。 |
| **深層予測・生成** | **SGCN** | 2D | - | 真に危険な対象のみにアテンションを向ける「疎な有向グラフ」で、人間の認知的取捨選択を獲得（強い）。 | 疎行列化により計算コストが著しく低下。エッジGPUでのリアルタイム並列推論に最適（極めて高い）。 | 【第1推奨】過密環境での推論効率を劇的改善しつつ、無関係なノイズに引きずられる過学習を防ぐ。 | 閾値マスキングのヒューリスティクスにより、背後からのランナー等を見落とすリスクが内在。 |
| **深層予測・生成** | **Trajectron++** | 2D+Map | Trajectron++ | CVAEと時空間グラフネットワークを介して詳細にモデリング。 | 複雑な構造と地図エンコーディングから計算負荷が高く、TensorRT等での最適化が必須（中）。 | 【実用標準】物理的に実現可能な制御入力分布を出力。地図情報や多種エージェントを統合可能。 | リアルタイム実行にはGPU最適化が必要で、パイプライン構築の難易度がやや高い。 |
| **深層予測・生成** | **AgentFormer** | 2D | AgentFormer | 時間次元と社会的次元を同時にモデリングし、社会力表現はトップクラス。 | Attentionの計算量が群衆環境で爆発的に増加するため極めて低く、ROS統合で大きな処理遅延を生む。 | 【長期予測】未来の相互作用まで考慮し、長期的な未来の連鎖反応を高精度に予測。 | 計算リソースへの要求が極めて高い。 |
| **深層予測・生成** | **MAVRIC** | 2D+Map | MAVRIC-Trajectron | (Trajectron++の派生形) | - | Trajectron++の派生形。 | (詳細な比較データなし)。 |
| **深層予測・生成** | **SceneAware** | 2D+Map | - | - | - | シーンの意味的理解（通行可能領域）を重視。 | (詳細記述なし)。 |
| **深層予測・生成** | **GFTD** | 2D | (arXiv:2303.16574) | - | - | 欠損データの補完（インペインティング）に強い。 | 実装コードが公開されていない可能性が高い。 |
| **深層予測・生成** | **DTDNet** | 2D | (PMC11091259) | - | - | 動的な目的地（Dynamic Target）を推定するネットワーク。 | 実装コードが公開されていない可能性が高い。 |
| **深層予測・生成** | **VISTA** | 2D+Map | (arXiv:2511.10203) | - | - | 視覚と意図を考慮したAttentionフレームワーク。 | 公式実装が見当たらない。 |
| **深層予測・生成** | **TrajCLIP** | 2D | - | - | - | 対照学習（CLIP）を用いた軌跡予測。 | 公式実装が見当たらない。 |
| **拡散・Transformer** | **MID / SingularTrajectory** | 2D | MID / SingularTrajectory | 相互作用や幾何学的制約への理解度が極めて高く、最高峰の社会力表現力を誇る。 | 逆拡散過程によるデノイジングの反復で致命的なレイテンシ。DDIM高速化してもエッジ負荷は重い（低い）。 | 【2025 SOTA】モード崩壊を回避し、現行最高精度の高品質で多様な軌跡分布を生成。 | 推論ステップ数が多く、リアルタイム化に課題。制御ループのボトルネックになり得る。 |
| **拡散・Transformer** | **Social-Transmotion** | 3D Pose | Social-Transmotion | 骨格や視線といった「言葉なき合図」から急激な意図変化を察知する理想的なHRIモデル（極めて高い）。 | 3D骨格検知等の重いセンシング前処理を要求するため、知覚・推論パイプラインが肥大化（最低）。 | 【意図理解】軌跡移動だけでは予測不可能な急な挙動（立ち止まりや振り返り）をいち早く察知。 | センシング要件が高く、現段階でのエッジ展開ハードルは高すぎる。 |
| **深層RL / 表現学習** | **SARL** (Social Attention) | 2D | CrowdNav | Self-Attentionで「危険な人」を特定し動的に重み付けする実用上十分な能力（中〜高）。 | 推論時のネットワーク計算量は小さく、エッジデバイス実行に適している（高い）。 | 【2D Nav推奨】局所計画と回避意思決定を高速実行。Freezing Robot回避に有効。DWAとのハイブリッドも有用。 | Sim-to-Realギャップが生じやすく、極めて複雑な連鎖反応の先読みには限界がある。 |
| **深層RL / 表現学習** | **GA3C-CADRL** | 2D | rl_collision_avoidance | 報酬に基づく暗黙的な学習。高度な社会的エチケットの明示的表現は弱くReward Shapingが必要（中）。 | 1回の推論（クエリ）に0.5ms未満しか要さない圧倒的速度。CPUでも20Hz以上の高周期ループに適合（極めて高い）。 | 非同期A3CとLSTMで任意の数の動的エージェントとの連続的相互作用を超高速に処理。 | Sim-to-Realギャップにより、歩行者ギリギリをすり抜ける非人間的な挙動に陥りやすい。 |
| **深層RL / 表現学習** | **Social-NCE** (表現学習) | - | social-nce (GitHub) | 負のサンプリングにより、衝突空間やパーソナルスペースへの侵入を潜在空間で強力な斥力として学習（極めて強い）。 | 自己教師ありの学習フレームワークであり、推論時の計算負荷はベースモデル（GA3C-CADRL等）に完全に依存する。 | 【安全性強化】既存モデルの学習プロセスに組み込むだけで、推論負荷を変えずに実機での衝突率を劇的に低下させる。 | 単体で動作せず、ベースモデルとの統合と再学習が必要。 |
| **深層RL / 表現学習** | **DS-RNN** | 2D | CrowdNav_DSRNN | 時空間グラフとRNNで動きの流れや過去の相互作用の記憶を明示的に考慮。 | RNNを使用する分計算量はSARLより若干増加するが、リアルタイム実行は十分可能。 | SARLの拡張。部分観測環境や高密度な群衆環境でのナビゲーション性能が向上。 | (特になし)。 |
| **深層RL / 表現学習** | **CADRL** | 2D | Social-Nav-RL | - | - | 深層RLによる衝突回避の草分け。 | 群衆（3人以上）の相互作用に弱い。 |
| **深層RL / 表現学習** | **SA-CADRL** | 2D | Social-Nav-RL | CADRLに社会的規範（左右通行等）を導入。 | - | CADRLの改良版。 | 入力サイズ固定の制限がある。 |
| **深層RL / 表現学習** | **R-SARL** | 2D | (Derived from CrowdNav) | - | - | SARLのリカレント拡張版。 | (SARLとの大きな差分情報なし)。 |
| **深層RL / 表現学習** | **RGL** (Relational Graph) | 2D | RelationalGraph | 人間同士の関係性も含めたグラフ推論。 | モデル構造が複雑で計算が重い。 | 高密度環境に強い。 | 計算コストが高い。 |
| **深層RL / 表現学習** | **Mapless DRL** | 2D LiDAR | Mapless-Nav | - | - | 2D LiDAR生データからのEnd-to-End学習。 | 机の天板など高さのある障害物に激突する。 |
| **深層RL / 表現学習** | **PointNet-RL** | 3D Cloud | PointCloud_RL | - | - | 【3D走破推奨】3D点群を直接理解し段差等を回避可能。 | Sim-to-Real学習の難易度が高い。 |
| **深層RL / 表現学習** | **Hybrid DRL** (DWA-RL) | 2D | DWA-RL | - | - | 【実用解】DWAの安全性をRLで動的調整。 | システム構成が複雑になる。 |
| **深層RL / 表現学習** | **DRL-VO** | 2D | drl_vo_nav | - | - | 【ハイブリッド】DRLで学習しつつVOで幾何学的な安全性も考慮。 | VOの制約を受けすぎると保守的になる可能性がある。 |
| **深層RL / 表現学習** | **SAC** / **TD3** | - | Stable-Baselines3 | - | - | 【RLアルゴリズム】連続値制御の汎用手法。 | (ナビ特化ではない汎用アルゴリズム)。 |
| **視覚・基盤モデル (LLM)** | **ViNT** | 2D Image | ViNT | 膨大なデータから「人を避ける」アフォーダンスを暗黙的に学習するが、明示的モデリングや幾何学的理解はない。 | 基盤モデルのパラメータ数が多いため、最適化（TensorRT等）が必須（低い）。 | 【視覚Nav標準】画像と目標地点のみから行動を出力する基盤モデルで、異なるロボットへのゼロショット転移が可能。 | 局所的な回避では衝突率が高まるケースがあり、DWA等のプランナー併用が強く推奨される。 |
| **視覚・基盤モデル (LLM)** | **DD-PPO** | 3D Visual | Habitat-Lab | - | - | 【視覚Nav】GPS+画像のみで未知環境を移動。超大規模分散学習。 | 学習に膨大な計算リソースが必要。 |
| **視覚・基盤モデル (LLM)** | **LM-Nav** | Text+Img | - | 「這裡是歩道」などの意味論的な環境の常識は理解できるが、群衆での物理的・動的な相互作用には関与しない（弱い〜中程度）。 | クラウドAPI通信や巨大モデルにより数秒単位のレイテンシが発生し、エッジでの実時間制御には不適（極めて低い）。 | 追加学習なし（ゼロショット）で自然言語の曖昧な指示に従い、複雑な屋外環境をナビゲーションできる。 | ランドマーク依存であり、動的な回避行動を秒単位で処理できない。 |
| **視覚・基盤モデル (LLM)** | **NavGPT / Co-NavGPT** | Text | NavGPT (GitHub) | 「前方に集団がいるから迂回する」等の意味論的・論理的な高次推論（マクロな社会力）は可能だが、瞬時の物理的回避には機能しない（中程度）。 | トークン管理、API遅延、実行負荷によりエッジデバイスでの実時間実行は困難。瞬時の反応不可（無〜極めて低い）。 | 複雑なサブゴールの分解など、論理的推論が可能でロボット行動の解釈性が極めて高い。 | 視覚情報のテキスト変換による幾何学的情報の欠落や、推論レイテンシが致命的。 |
| **プラットフォーム** | **Arena-Rosnav** | System | Arena-Rosnav | - | - | 【開発環境】RLエージェントの学習・評価・比較を行う統合環境。 | 環境構築にGPU等が必要。 |