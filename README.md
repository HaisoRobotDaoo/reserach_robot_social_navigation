### 📋 自律移動ロボットにおける人流経路予測および人流回避アルゴリズム 統合評価表

| カテゴリ | モデル名 | 入力 | URL / リポジトリ | 🌟社会力（相互作用の考慮） | 🌟エッジ適性（リアルタイム性） | メリット・推奨理由 | デメリット・課題 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **古典的物理・数理** | **SFM** | 2D | `PySocialForce` (リポジトリ名) | パーソナルスペース維持のための反発力を数式としてモデリングしているが、意図や譲り合いといった高次な表現力は弱い。 | 単純なベクトル演算のみのため、CPU環境でもミリ秒単位での実行が可能で極めて高い。 | 【Sim標準】群衆シミュレータの標準エンジンとして機能。計算が極めて軽い。 | 反応型で予見的回避不可。狭所で不自然な振動（オシレーション）や膠着（デッドロック）に陥りやすい。 |
| **古典的物理・数理** | **VO** | 2D | (概念モデル) | 静的な回避になりがち（ORCAの前身）。 | 高速計算可能。 | 衝突する速度領域を幾何学的に定義。 | 単体での実装は稀。 |
| **古典的物理・数理** | **ORCA / RVO2** | 2D | `RVO2 Library` (リポジトリ名) | 「互いに避け合う（責任折半）」という社会的な暗黙のルールを数学的な制約に落とし込んでいる（中程度）。 | 線形計画法のソルバが極めて軽量。数千エージェントでも数ミリ秒で計算可能で、極めて高い。 | 【2D決定版】数学的に衝突フリーを保証し振動を抑制。AI暴走を防ぐ安全フィルタとして必須。 | 相手が協力的でないと機能不全に陥り、不自然な急制動を引き起こすリスクがある。 |
| **古典的物理・数理** | **DWA** | 2D | `ROS Navigation` / `dwa_local_planner` | 歩行者を動的な障害物として扱うのみで、相互作用のモデリングは存在しない（無）。 | サンプリングベースの探索で計算負荷は低く、軽量エッジデバイスでも動作する。 | 【ROS標準】ロボットのキネマティクス制約を厳密に考慮し、実機追従性が高い。 | 動的障害物の未来予測能力がなく、急停止する「Freezing Robot Problem」を引き起こしやすい。 |
| **古典的物理・数理** | **TEB** | 2D | `teb_local_planner` (リポジトリ名) | 歩行者を動的な障害物ペナルティとして扱うのみで、相互作用のモデリングは弱い。 | グラフ最適化の反復計算コストが高く、エッジデバイスでは最適化がタイムアウトし処理遅延になり得る。 | 【滑らかさ重視】キネマティクス制約を満たしながら、経路をゴムバンドのように変形させ時間最適な軌道を生成。 | 複雑な環境では局所解に陥りやすい。 |
| **古典的物理・数理** | **CBF** | 2D/3D | `CBF_QP_safety_filter` (リポジトリ名) | 数学的な安全制約として機能。 | 安全フィルタとして計算コストは極めて低い。 | 【安全性保証】数理的に安全領域を担保し、AIの暴走を防ぐフェールセーフとして機能。 | 設定次第で挙動が保守的になりすぎる可能性がある。 |
| **古典的物理・数理** | **DRA-MPPI** | 2D/3D | `DRA-MPPI` (リポジトリ名) | 外部の深層予測モデルが出力する不確実な未来分布をコスト関数に直接組み込める（外部依存で高い）。 | 毎ステップ数百の軌道を評価しモンテカルロ積分を行うため、GPU並列演算（CUDA）が事実上必須（中程度）。 | 【制御SOTA】非ガウス型の動的障害物予測に対応し、過度に保守的な挙動やFreezing Robotを軽減。 | サンプリングと積分による計算負荷が極めて高く、エッジリソースを大きく消費する。 |
| **古典的物理・数理** | **Boids** | - | **[PredatorPrey](https://github.com/piskvorky/PredatorPrey)** | 距離に基づく分離・整列・結合のみで、高次な社会的ルール（パーソナルスペースや譲り合い）はない（低い）。 | 単純演算でO(N log N)以下に最適化可能。マイコンレベルでも極めて高速。 | 局所的ルールの積み重ねで、滑らかに障害物を避け合流する創発的振る舞いを極低計算で再現。 | 各個体に明確な目的地がなく、迷路やデッドエンドからの脱出が困難。 |
| **古典的物理・数理** | **セルオートマトン(フロアフィールド)** | 2D Map | - | 仮想フェロモンにより「流れに乗る」等の暗黙の群衆ルールを間接的にモデリング（中程度）。 | 局所的ルールの並列計算であり、GPU並列化と極めて相性が良く非常に軽い。 | レーン形成やアーチ形成など、マクロな自己組織化現象の再現に非常に優れる。 | 空間・時間の離散化により軌道が不自然であり、平滑化処理が必須。 |
| **伝統的機械学習** | **KF / EKF** | - | `FilterPy` (リポジトリ名) | - | 非常に軽量。 | 【状態推定】ノイズ除去と平滑化の基本技術。 | 急な意図変化（ターン等）には追従できない。 |
| **伝統的機械学習** | **GP** | 2D | `GPy` (リポジトリ名) | 単一エージェントの時系列モデリングに留まり、相互作用の把握は困難。 | 計算量がデータ数の3乗で増加するため、エッジ実行は事実上困難。 | 【不確実性】予測の「確信度（分散）」を厳密に出力可能。 | 計算量の爆発によりリアルタイム処理が極めて困難。 |
| **伝統的機械学習** | **IRL** | 2D | `Social-Nav-RL` (リポジトリ名) | 人間の軌跡からパーソナルスペース等の社会的規範を逆推定する強力な枠組み（強い）。 | オンラインで動的環境に適応しながら強化学習ループを解くことは非現実的。 | 【規範学習】人間らしいナビゲーションの規範を抽出できる。 | 計算コストが極大でリアルタイム学習は困難。 |
| **伝統的機械学習** | **HMM** | 2D | - | 単一意図推論に特化し、多人数間の相互作用は次元の呪いにより困難（弱い）。 | オフライン学習は重いが、オンライン推論（Viterbi）は軽量でCPUでも高い。 | センサノイズにロバストで、急な意図変化を確率遷移として自然に扱える。 | マルコフ性の仮定により長期予測で誤差が発散しやすい。 |
| **深層予測・生成** | **Social-LSTM** | 2D | `Traj Pred Models` (リポジトリ名) | Social Poolingにより周囲の隠れ状態を集約。 | 逐次処理とプーリングの計算が重くエッジ向きではない。 | RNNベースの軌跡予測の先駆的モデル。 | 計算が重く、多様性表現（Multimodality）に乏しい。 |
| **深層予測・生成** | **Social-GAN** | 2D | `sgan` (リポジトリ名) | Social-LSTM同様のプーリング機構。 | 歩行者数増大で推論負荷が線形以上に増大する。 | GANにより複数の未来軌跡を生成可能。 | 学習が不安定でモード崩壊のリスクがある。 |
| **深層予測・生成** | **SoPhie** | 2D+Map | **[sophie](https://github.com/coolsunxu/sophie)** | Social / Physical Attentionにより、人間同士と環境の複合的なルールを高度に捉える（極めて強い）。 | 画像処理(CNN)とLSTM計算が重く、CPUでは数十〜数百msの遅延。GPU必須（低〜中）。 | 物理的制約（壁抜け等）を削減し、障害物回避におけるマルチモーダルな挙動を再現。 | アーキテクチャが重厚でエッジリソースを多く消費する。 |
| **深層予測・生成** | **Social-BiGAT** | 2D+Map | - | グラフアテンションにより「遠いが衝突軌道にある人」へ動的に強いアテンションを割り当てる（極めて強い）。 | LSTM+GAT+GANの構造が並列化を阻害し、制御周期への追従が極めて困難（低い）。 | プーリングによる情報欠落を防ぎ、モード崩壊を回避して多様な軌跡を予測。 | モデルが極めて複雑で計算コストが膨大。 |
| **深層予測・生成** | **STAR** | 2D | **[STAR](https://github.com/cunjunyu/STAR)** | 空間・時間独立のTransformerにより、対向・並走などの空間的インタラクションを効果的にモデリング（強い）。 | 並列化効率は高いが、密集環境ではアテンション計算がO(N^2)で増大するため強力なGPUが必要（中〜高）。 | 長期予測に優れ、外部グラフメモリにより遮蔽等の影響を滑らかに維持。 | パラメータ数が多く、密集環境での計算量増加が著しい。 |
| **深層予測・生成** | **PECNet** | 2D | **[PECNet](https://github.com/onuramca/PECNet)** | 他者の「予測された終点」を考慮したソーシャルプーリングを行うが、微細な相互作用解像度はやや劣る（中程度）。 | サンプリングと中間軌跡の一括生成は比較的軽量で安定している（中程度）。 | 目的地（終点）を先に予測することで長期予測の誤差発散を根本から防ぐ。 | 終点予測が外れると中間軌跡が乖離し、急な目的地変更への追従性が低い。 |
| **深層予測・生成** | **Social-STGCNN** | 2D | `Social-STGCNN` (リポジトリ名) | 相互作用を明示的に表現するが、単純な距離ベースのため複雑な関係性は捉えきれない（高）。 | RNN排除で極めて軽く、パラメータ7.6K、500Hz推論。非力なエッジでも動作（極めて高い）。 | 【高速】計算速度が圧倒的で、予測エンジンとしてDWA等との組み合わせで現在最もバランスが良い。 | Transformer系等に比べ長期予測精度が劣る傾向。 |
| **深層予測・生成** | **SGCN** | 2D | - | 真に危険な対象のみにアテンションを向ける「疎な有向グラフ」で、選択的集中を表現（強い）。 | 疎行列化により計算コストが著しく低下。エッジGPUでのリアルタイム並列推論に最適（極めて高い）。 | 【第1推奨】過密環境での計算効率(MACs)を劇的改善し、無関係なノイズによる過学習を防ぐ。 | 閾値マスキングにより、背後からのランナー等を見落とすリスクが内在。 |
| **深層予測・生成** | **Trajectron++** | 2D+Map | `Trajectron++` (リポジトリ名) | CVAEと時空間グラフを介して詳細にモデリング。 | 複雑な構造と地図から計算負荷が高く、TensorRT等での最適化が必須（中）。 | 【実用標準】物理的に実現可能な制御入力分布を出力。地図情報や異種エージェントを統合可能。 | リアルタイム実行にはGPU最適化が必要で実装難易度がやや高い。 |
| **深層予測・生成** | **AgentFormer** | 2D | `AgentFormer` (リポジトリ名) | 時間次元と社会的次元を同時にモデリングし、社会力表現はトップクラス。 | Attentionの計算量が群衆環境で爆発的に増加するため極めて低く、ROS統合で遅延を生む。 | 【長期予測】未来の相互作用まで考慮し、長期的な未来の連鎖反応を高精度に予測。 | 計算リソースへの要求が極めて高い。 |
| **深層予測・生成** | **MAVRIC** | 2D+Map | `MAVRIC-Trajectron` (リポジトリ名) | (Trajectron++の派生形) | - | Trajectron++の派生形。 | (詳細な比較データなし)。 |
| **深層予測・生成** | **SceneAware** | 2D+Map | - | - | - | シーンの意味的理解を重視。 | (詳細記述なし)。 |
| **深層予測・生成** | **GFTD** | 2D | 論文: arXiv:2303.16574 | - | - | 欠損データの補完に強い。 | 実装コード非公開の可能性。 |
| **深層予測・生成** | **DTDNet** | 2D | 論文: PMC11091259 | - | - | 動的な目的地を推定。 | 実装コード非公開の可能性。 |
| **深層予測・生成** | **VISTA** | 2D+Map | 論文: arXiv:2511.10203 | - | - | 視覚と意図を考慮したAttention。 | 公式実装が見当たらない。 |
| **深層予測・生成** | **TrajCLIP** | 2D | - | - | - | 対照学習（CLIP）を用いた予測。 | 公式実装が見当たらない。 |
| **拡散・Transformer** | **MID / SingularTrajectory** | 2D | `MID` / `SingularTrajectory` (リポジトリ名) | モード崩壊を回避し、現行最高峰の社会力・多様な軌跡分布を生成（極めて高い）。 | 逆拡散過程で致命的なレイテンシ。DDIM高速化してもエッジ負荷は重い（低い）。 | 【2025 SOTA】最高精度の高品質で多様な軌跡分布を生成。 | 推論ステップ数が多く、制御ループのボトルネックになり得る。 |
| **拡散・Transformer** | **Social-Transmotion** | 3D Pose | `Social-Transmotion` (リポジトリ名) | 骨格や視線等の「言葉なき合図」から急激な意図変化を察知（極めて高い）。 | 3D骨格検知等の重い前処理を要求しパイプラインが肥大化（最低）。 | 【意図理解】軌跡移動だけでは予測不可能な急な挙動（振り返り等）を察知。 | センシング要件が高く、エッジ展開ハードルは現時点で高すぎる。 |
| **深層RL / 表現学習** | **SARL** | 2D | `CrowdNav` (リポジトリ名) | Attentionで「危険な人」に重み付けし、自然な回避行動を生成（中〜高）。 | 計算量は小さく、エッジデバイス実行に適している（高い）。 | 【2D Nav推奨】局所計画と回避意思決定を高速実行。Freezing Robot回避に有効。 | 極めて複雑な連鎖反応の先読みには限界がある。 |
| **深層RL / 表現学習** | **GA3C-CADRL** | 2D | **[rl_collision_avoidance](https://github.com/mit-acl/rl_collision_avoidance)** | 報酬に基づく暗黙的な学習。明示的な社会規範は弱くチューニングが必要（中）。 | 1回の推論に0.5ms未満。CPUでも20Hz以上の制御ループに適合（極めて高い）。 | 直接制御を出力するため超高速。任意の数のエージェントを処理。 | Sim-to-Realギャップにより、歩行者ギリギリをすり抜ける非人間的挙動に陥りやすい。 |
| **深層RL / 表現学習** | **Social-NCE** | - | **[social-nce](https://github.com/vita-epfl/social-nce)** | 衝突空間やパーソナルスペースを負の斥力として強力に学習（極めて強い）。 | 推論時の計算負荷はベースモデル（GA3C-CADRL等）に完全に依存する。 | 【安全性強化】既存モデルの推論負荷を変えずに衝突率を劇的に低下させる。 | 単体で動作せず、ベースモデルとの統合と再学習が必要。 |
| **深層RL / 表現学習** | **DS-RNN** | 2D | `CrowdNav_DSRNN` (リポジトリ名) | 時空間グラフ+RNNで過去の相互作用の記憶を明示的に考慮。 | RNN使用によりSARLより若干計算は増えるがリアルタイム実行可能。 | 部分観測環境や高密度な群衆でのナビゲーション性能が向上。 | (特になし)。 |
| **深層RL / 表現学習** | **CADRL / SA-CADRL** | 2D | `Social-Nav-RL` (リポジトリ名) | 深層RLによる回避の草分け / 社会的規範の導入。 | - | - | 群衆の相互作用に弱い / 入力サイズ固定制限。 |
| **深層RL / 表現学習** | **RGL** | 2D | `RelationalGraph` (リポジトリ名) | 人間同士の関係性も含めたグラフ推論。 | モデル構造が複雑で計算が重い。 | 高密度環境に強い。 | 計算コストが高い。 |
| **深層RL / 表現学習** | **Mapless DRL** | 2D LiDAR | `Mapless-Nav` (リポジトリ名) | - | - | 2D LiDARからのEnd-to-End学習。 | 机の天板など高さのある障害物に激突する。 |
| **深層RL / 表現学習** | **PointNet-RL** | 3D Cloud | `PointCloud_RL` (リポジトリ名) | - | - | 【3D走破推奨】3D点群を直接理解し回避。 | Sim-to-Real学習難易度が高い。 |
| **深層RL / 表現学習** | **Hybrid DRL** | 2D | `DWA-RL` (リポジトリ名) | - | - | 【実用解】DWAの安全性をRLで動的調整。 | システム構成が複雑になる。 |
| **深層RL / 表現学習** | **DRL-VO** | 2D | `drl_vo_nav` (リポジトリ名) | - | - | 【ハイブリッド】DRLとVOで幾何学的安全性考慮。 | VOの制約を受けすぎると保守的になる。 |
| **深層RL / 表現学習** | **SAC / TD3** | - | `Stable-Baselines3` (リポジトリ名) | - | - | 連続値制御の汎用RL手法。 | ナビ特化ではない。 |
| **視覚・基盤モデル** | **ViNT** | 2D Image | `ViNT` / `visualnav-transformer-ros2` | 「人を避ける」アフォーダンスを暗黙に学習するが、明示的モデリングはない。 | 基盤モデル特有の重さから、最適化（TensorRT等）が必須（低い）。 | 【視覚Nav標準】画像と目標地点のみから行動を出力し、ゼロショット転移可能。 | 局所的な回避では衝突率が高まる場合があり、プランナー併用が推奨。 |
| **視覚・基盤モデル** | **DD-PPO** | 3D Visual | `Habitat-Lab` (リポジトリ名) | - | - | 【視覚Nav】GPS+画像のみで未知環境を移動。 | 学習に膨大な計算リソースが必要。 |
| **視覚・基盤モデル** | **LM-Nav** | Text+Img | - | 意味論的な常識は理解できるが、群集での物理的・動的相互作用には関与しない（弱い）。 | クラウドAPI通信等で数秒のレイテンシが発生し、エッジ実時間制御に不適（極めて低い）。 | ゼロショットで自然言語の指示に従い、複雑な屋外環境をナビゲーション可能。 | ランドマーク依存で、動的な回避行動を処理できない。 |
| **視覚・基盤モデル** | **NavGPT / Co-NavGPT** | Text | **[NavGPT](https://github.com/GengzeZhou/NavGPT)** | 「前方に集団がいるから迂回」等の意味論的推論は可能だが、物理的回避には機能しない（中）。 | トークン管理・API遅延によりエッジでの実時間実行は困難（無〜極めて低い）。 | サブゴール分解など論理的推論が可能で、行動の解釈性が極めて高い。 | 視覚のテキスト変換による幾何学情報の欠落や、推論レイテンシが致命的。 |
| **プラットフォーム** | **Arena-Rosnav** | System | `Arena-Rosnav` (リポジトリ名) | - | - | RLエージェントの学習・評価・比較環境。 | 環境構築にGPU等が必要。 |